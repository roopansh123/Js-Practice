function fn() {
  console.log("hello");
}

// function throttle(fn, delay) {
//   let lastcall = 0;

//   return function () {
//     let now = Date.now();
//     if (now - lastcall >= delay) {
//       lastcall = now;
//       fn();
//     } else {
//       console.log("plz call after some time");
//     }
//   };
// }

// let throt = throttle(fn, 1);

// throt();

// function bubblesort(arr) {
//   let n = arr.length;
//   for (let i = 0; i < n; i++) {
//     for (let j = 0; j < n - i - 1; j++) {
//       if (arr[j] > arr[j + 1]) {
//         let temp = arr[j];
//         arr[j] = arr[j + 1];
//         arr[j + 1] = temp;
//       }
//     }
//   }
// }

// let arr = [4, 8, 6, 7, 1];
// console.log(arr);

// bubblesort(arr);
// console.log(arr);

// function devounce(fn, delay) {
//   let interval = null;

//   return function () {
//     if (interval != null) {
//       clearTimeout(interval);
//       console.log("already running");
//     }
//     interval = setTimeout(() => {
//       fn();
//     }, delay);
//   };
// }

// let debounce = devounce(fn, 1000);

// debounce();
// debounce();
// debounce();
// debounce();

// debounce();
// debounce();


function selectionsort(arr){
  for (let i =0 ; i < n ; i++){
    
    for ()
  }
}